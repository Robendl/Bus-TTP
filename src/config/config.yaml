defaults:
  - override hydra/job_logging: disabled  # optional: quiet logging

project_name: 'bus-ttp'

model:
  mlp:
    hidden_dims: [64, 128]
  output_dim: 1

training:
  test_size: 0.2
  random_state: 42
  epochs: 100
  batch_size: 64
  learning_rate: 1e-3
  eval_frequency: 0.2
