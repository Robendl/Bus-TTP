defaults:
  - override hydra/job_logging: disabled  # optional: quiet logging

project_name: 'bus-ttp'

model:
  mlp:
    hidden_dims: [64, 32]
  output_dim: 1

training:
  dataset: 'training_13juni_100p.csv'
  test_size: 0.2
  random_state: 42
  epochs: 20
  batch_size: 256
  learning_rate: 1e-3
  eval_frequency: 1
